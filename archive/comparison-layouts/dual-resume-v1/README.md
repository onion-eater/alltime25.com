# Dual résumé comparison v1

Historical reference only. These files are exact copies of the active
comparison implementation at commit `9ec3c15`.

Original locations:

- `CompareScreen.tsx` and `CompareScreen.module.css`:
  `apps/web/src/features/ranking/components/`
- `DesktopResumeCard.tsx` and `DesktopResumeCard.module.css`:
  `apps/web/src/features/ranking/components/`
- `MobileComparisonTable.tsx` and `MobileComparisonTable.module.css`:
  `apps/web/src/features/ranking/components/`
- `statDefinitions.ts`: `apps/web/src/features/ranking/model/`

The archive is outside every application source root. Active code must not
import, compile, lint, test, or bundle these files.
