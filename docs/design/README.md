# Design artifacts

- `concepts/`: approved historical visual references.
- `renders/`: implementation captures from the approved comparison design.
- `source/`: editable historical prototype source.

These artifacts preserve design decisions; they are not production code. The
prototype may include interactions that were superseded in the React
application. The live application and its automated tests are canonical for
current behavior.
